apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: arr-stack

resources:
    - namespace.yaml
    - gluetun-sealed-secret.yaml
    - pvc.yaml
    - nas-pvc.yaml
    - deployment.yaml
    - services.yaml
    - ingress.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: arr-stack

# Für Prod-Overlay: Images mit Digest pinnen
# images:
#   - name: qmcgaw/gluetun
#     newTag: v3.38.0

# ConfigMap aus .env Datei generieren (Alternative zu secrets.yaml)
# secretGenerator:
#   - name: gluetun-vpn-secrets
#     envs:
#       - .env
#     options:
#       disableNameSuffixHash: true
